apiVersion: v1
kind: Secret
metadata:
  name: postgres-mcp-secret
  namespace: default
type: Opaque
stringData:
  POSTGRES_USER: "your_username"
  POSTGRES_PASSWORD: "your_password"
# Note: In production, create this secret using kubectl:
# kubectl create secret generic postgres-mcp-secret \
#   --from-literal=POSTGRES_USER=your_username \
#   --from-literal=POSTGRES_PASSWORD=your_password


# PostgreSQL MCP Server Configuration
# Copy this file to .env and update with your values

# PostgreSQL Connection Settings
POSTGRES_HOST=mcp-postgres-db
POSTGRES_PORT=5432
POSTGRES_DB=demodb
POSTGRES_USER=demouser
POSTGRES_PASSWORD=demo123

# Server Settings (Optional - defaults shown)
# SERVER_HOST=0.0.0.0
# SERVER_PORT=8100

# Production Security Notes:
# 1. Never commit the actual .env file
# 2. Use strong passwords in production
# 3. Consider using Docker secrets or Kubernetes secrets
# 4. Enable SSL/TLS for PostgreSQL connections in production


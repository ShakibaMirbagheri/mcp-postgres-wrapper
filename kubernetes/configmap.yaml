apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-mcp-config
  namespace: default
data:
  POSTGRES_HOST: "your-postgres-host"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "your_database"
  # Note: Add sensitive data in Secret, not ConfigMap

